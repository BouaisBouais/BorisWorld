<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="WPF.OkCancelDialog.OKCancelDialog"
    Title="Confirmation"
    WindowStartupLocation="CenterOwner"
    WindowStyle="ToolWindow"
    ResizeMode="NoResize"
    UseLayoutRounding="True"
    TextOptions.TextFormattingMode="Display" Height="102" Width="360">

    <Grid Margin="0,0,0,-1">
        <Button
            x:Name="OKButton"
            Content="OK"
            Height="23"
            HorizontalAlignment="Right"
            Margin="0,0,93,12"
            VerticalAlignment="Bottom"
            Width="75"
            Click="OKButton_Click"
            IsDefault="True"
            Command="{Binding OKButtonCommand}" />

        <Button
            x:Name="CancelButton"
            Content="Cancel"
            Height="23"
            HorizontalAlignment="Right"
            Margin="0,0,12,12"
            VerticalAlignment="Bottom"
            Width="75"
            IsCancel="True" />
        <TextBlock x:Name="LabelInfo" Margin="10,10,12,0" VerticalAlignment="Top" Height="23"/>
    </Grid>
</Window>